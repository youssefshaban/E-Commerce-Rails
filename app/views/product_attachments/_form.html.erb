<%= form_with(model: product_attachment, local: true) do |form| %>
  <% if product_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_attachment.errors.count, "error") %> prohibited this product_attachment from being saved:</h2>

      <ul>
      <% product_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
