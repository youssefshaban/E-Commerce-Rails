<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table class="table table-dark table-hover">
  <thead>
    <tr>
      <th>ID</th>
      <th>State</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.state %></td>
        <td><%= link_to 'Show', order, class: 'btn btn-outline-info'  %></td>
        </tr>
    <% end %>
  </tbody>
</table>

<br>

